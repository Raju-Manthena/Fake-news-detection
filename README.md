Training ml models to detect if given news is real or fake. Implemented logistic regression(shortcut and theoretical), Decision tree,Xg boost and Random tree classifiers.
Pre processing of textual data
